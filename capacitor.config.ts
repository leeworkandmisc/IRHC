import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'IRHC',
  appName: 'Infrared Heating Calculator',
  webDir: 'https',
  bundledWebRuntime: false,
};

export default config;
